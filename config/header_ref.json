{ 
    "header_ref":{
      "custom": {
            "required_fields": ["categories", "specific"]
        },
        "base": {
            "required_fields": ["roles", "devices", "stage","specific"]
        }
    },
  "allowed_values": {
    "roles": ["developer", "metrolog" ],
    "stage":[]
    }
}